#include <iostream>
using namespace std;

int main() {
    int a;
    std::cin >> a;
    int b;
    std::cin >> b;
    
    int c;
    std::cin >> c;
    
    int d;
    std::cin >> d;
if(0<a && a <= 225 && 0<b &&  b <= 225 && 0<c && c<=225 && 0<d&& d<=225 ){
     std::cout <<a<<"."<<b<<"."<<c<<"."<<d;
}
else{
    
}

    return 0;
}